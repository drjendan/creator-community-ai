import { Building2, CircleDollarSign, MessageSquareWarning, Sparkles, Users } from "lucide-react";
import { StatCard } from "@/components/dashboard/StatCard";
import { Card, CardTitle } from "@/components/ui";
const stats = [["Total tenants", "28", Building2], ["Active subscriptions", "24", CircleDollarSign], ["Monthly recurring revenue", "$3,948", CircleDollarSign], ["Total members", "12.8K", Users], ["AI usage", "2.4M tokens", Sparkles], ["Support requests", "7 open", MessageSquareWarning]] as const;
export default function PlatformAdminPage() { return <div className="space-y-8"><div><p className="text-xs font-bold uppercase tracking-wide text-accent-700">Platform operations</p><h1 className="mt-2 font-display text-3xl font-extrabold text-brand-900">Overview</h1><p className="mt-2 text-sm text-brand-600">Monitor tenants, subscriptions, usage, and service health across UpNexx.</p></div><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{stats.map(([label, value]) => <StatCard key={label} label={label} value={value} />)}</section><div className="grid gap-5 xl:grid-cols-2"><Card><CardTitle>Tenant growth</CardTitle><div className="mt-5 flex h-48 items-end gap-3">{[45, 52, 48, 64, 71, 78, 86, 96].map((height, index) => <span key={index} className="flex-1 rounded-t bg-accent-500" style={{ height: `${height}%` }} />)}</div></Card><Card><CardTitle>Platform health</CardTitle><div className="mt-5 space-y-4">{[["Application", "Operational"], ["Database", "Operational"], ["Background jobs", "Operational"], ["AI providers", "Monitoring"]].map(([name, status]) => <div key={name} className="flex items-center justify-between rounded-lg border border-brand-100 p-3"><span className="text-sm font-semibold text-brand-700">{name}</span><span className="rounded-full bg-success-soft px-2.5 py-1 text-xs font-bold text-success-strong">{status}</span></div>)}</div></Card></div></div>; }

